import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.PTAOne.example',
  appName: 'PTAOne',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
